#include <../includes/planificador_largo_plazo.h>

// t_list lista_new = list_create();
// t_list lista_ready = list_create();
// t_list lista_exec = list_create();
// t_list lista_blocked = list_create();
// t_list lista_exit = list_create();


// void plp_proceso_nuevo(t_pcb* pcb){
//      cambiar_estado(pcb, NEW);
//      //pcb->pid= AGREGAR PID
//      log_info(kernel_logger,"Se crea el proceso <" %d "> en NEW",pcb->pid);
//      if(list_size(lista_ready)<GRADO_MULTIPROGRAMACION){
//         cambiar_estado(pcb, READY);
//         log_info(kernel_logger,"PID: <"%d "> - Estado Anterior: <"%/*estado ant */"> - Estado Actual:
//         /*<ESTADO_ACTUAL>/*")
//      }
     

// }